# AgentClinic

Spec-driven sample: AI agents find relief from their humans (dashboard, ailments, therapies, booking).

## Development

```bash
npm install
npm run dev        # start server (default http://localhost:3000)
npm run typecheck  # TypeScript validation
npm test           # Vitest — routes, stylesheet, layout structure
```
